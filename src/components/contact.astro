<section id="contact" class="w-full py-12 border-t border-[#ffffff10]">
  <div class="max-w-5xl mx-auto">
    <h2 class="text-lg text-[var(--sec)] mb-2 shiny-sec">Parlons ensemble !</h2>
    <h3 class="text-4xl md:text-5xl font-medium text-[var(--white)] mb-6">
      Contact
    </h3>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div class="text-[var(--white-icon)]">
        <p class="mb-4">
          Une question ? un projet en tête ? nous pouvons en parler ! N'hésitez
          pas à me contacter via le formulaire ci-dessous ou par email.
        </p>
        <div class="flex items-center gap-2">
          <span>Emplacements:</span>
          <span class="text-[var(--white)]">
            Orléans, France / Chatillon, France</span
          >
        </div>
      </div>

      <div>
        <form id="contactForm" class="flex flex-col gap-4">
          <input
            type="text"
            name="from_name"
            class="px-4 py-2 bg-[#1414149c] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)]"
            placeholder="Votre nom"
            required
          />
          <input
            type="email"
            name="reply_to"
            class="px-4 py-2 bg-[#1414149c] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)]"
            placeholder="Votre email"
            required
          />
          <textarea
            name="message"
            class="px-4 py-2 bg-[#1414149c] text-[var(--white)] border border-[var(--white-icon-tr)] rounded-lg focus:outline-none focus:ring-2 focus:ring-[var(--sec)] resize-none"
            placeholder="Votre message"
            required></textarea>
          <button
            type="submit"
            class="px-4 py-2 bg-[var(--white-icon-tr)] text-[var(--white)] rounded-lg opacity-60 transition-opacity border border-[var(--white-icon-tr)] hover:opacity-100 hover:bg-[var(--white-icon-tr)]"
            >Envoyer</button
          >
        </form>
        <div
          id="form-message"
          class="hidden justify-center items-center mt-4 text-[var(--white)] text-lg"
        >
          ✅ Thank you for your message!
        </div>
      </div>
    </div>
  </div>
  <br />
  <div></div>
</section>

<script>
  const form = document.getElementById("contact-form");
  const formMessage = document.getElementById("form-message");
  const scriptURL = "https://script.google.com/macros/s/AKfycbwrdTKcRJ2Z6eyKY6G8mJT-OqZ2Pi_srpYLDGYu9Q_LSCOK3a2TB7S1TODlbhm9Alir/exec";

  document.getElementById("contactForm").addEventListener("submit", e => {
    e.preventDefault();
    const form = e.target as HTMLFormElement;
    const formData = new FormData(form);
    fetch(scriptURL, { method: "POST", body: formData })
      .then(response => {
        form.reset();
        formMessage.textContent = "✅ Message envoyé avec succès !";
        formMessage.classList.remove("hidden");
        setTimeout(() => formMessage.classList.add("hidden"), 4000);
      })
      .catch(error => {
        formMessage.textContent = "❌ Erreur lors de l'envoi : " + error;
        formMessage.classList.remove("hidden");
        setTimeout(() => formMessage.classList.add("hidden"), 4000);
      });
  });
</script>
